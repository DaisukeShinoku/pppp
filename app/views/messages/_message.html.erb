<div class='message'>
<% if message.user.id == current_user.id %>
  <%# 投稿者を特定できるようにメールアドレスを表示させておく %>
  <div class="left-message"><%= "#{message.user.email}: #{message.content}" %></div>
<% else %>
  <div class="right-message"><%= "#{message.user.email}: #{message.content}" %></div>
<%end%>
</div>